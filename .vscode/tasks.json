{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/src/TechJobsConsole/TechJobsConsole.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}